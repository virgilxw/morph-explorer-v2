import React from 'react';

const CollaspeBar = ({ isVisible, toggleVisibility }) => {
  return (
    <aside className={`transition-all duration-300 w-0.5 bg-gray-400 px-2 overflow-hidden`}>
    </aside>
  );
};

export default CollaspeBar;
